#include <stdio.h>
/**
 * main - main function that print name
 * @argv:value of array
 * @argc:count number of element
 * Return:0
 */
int main(int __attribute__((__unused__)), argc, char *argv[])
{
printf("%s \n", argv[0]);
return (0);
}
