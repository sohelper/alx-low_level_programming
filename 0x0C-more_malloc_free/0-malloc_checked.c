#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 * *malloc_checked -  function that allocates memory using malloc
 * @b:unsigned int
 * Return: value of 98
 */
void *malloc_checked(unsigned int b)
{
int x;
void *x = malloc(b);
if (x == NULL)
{
return (98);
}
else
{
return (p);
}
}
